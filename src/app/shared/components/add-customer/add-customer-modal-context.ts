// this represents the internal context passed from the modal SPA to the internal SKY UX modal component
export class AddCustomerModalContext {
  public envId: string;
  public first: string;
  public last: string;
}
